{
  "name": "nano-account-crawler",
  "version": "2.0.1",
  "description": "Makes block iterators that make multiple RPC calls to Nano nodes if neccessary. Block confirmation height is checked so only confirmed blocks are iterated.",
  "repository": "https://github.com/Airtune/nano-account-crawler",
  "files": [
    "dist/**/*.*",
    "src/**/*.*"
  ],
  "scripts": {
    "build": "npx tsc --build tsconfig.json",
    "test": "mocha --require ts-node/register ./test/*.spec.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@bananocoin/bananojs": "^2.8.7",
    "@types/mocha": "^9.1.0",
    "@types/node": "^18.15.5",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "chai-as-promised": "^7.1.1",
    "node-fetch": "^2.6.7"
  }
}
